=> first we will going to create file called 'viewsets.py' inside 'products'
=> we can be able to add viewsets inside 'views.py' but right now it is quite full so we will create it's own file
=> now again let we create 'routers.py' file inside 'cfehome'
=> and again we can put routers inside urls.py but right now we will create it's own file
=> after creating router we will going to add that router inside 'cfehome/urls.py'